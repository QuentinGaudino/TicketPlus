<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {# Run `composer require symfony/webpack-encore-bundle`
           and uncomment the following Encore helpers to start using Symfony UX #}
        {% block stylesheets %}
            {#{{ encore_entry_link_tags('app') }}#}
        {% endblock %}

        {% block javascripts %}
            {#{{ encore_entry_script_tags('app') }}#}
        {% endblock %}
    </head>
    <header>
        <ul>
            <li>La date</li>
            <li>Les notifs</li>
            <li>L'utilsateur</li>
        </ul>
        <a href="{{ path('ticket_crud_new_demand') }}">Nouvelle demande</a>
        <a href="{{ path('ticket_crud_new_incident') }}">Nouvel incident</a>
        <a href="{{ path('ticket_crud_all_incidents') }}">Tout les incidents</a>
        <a href="{{ path('ticket_crud_my_tickets', {slug: 1}) }}">Mes demandes</a>
        <a href="{{ path('ticket_crud_my_tickets', {slug: 2}) }}">Mes incidents</a>
    </header>
    <body>
        {% block body %}{% endblock %}
    </body>
</html>
