{% extends 'base.html.twig' %}

{% block title %}Compte utilisateur{% endblock %}

{% block body %}
    <h1 class="title is-2">Compte utilisateur</h1>

    {{ include('user/_form.html.twig', {'button_label': 'Update'}) }}

    <div class="container mt-3 is-flex is-justify-content-flex-end">
        <a href="{{ path('user_edit_password', {'id' : id}) }}" class="button is-link mr-3">Changer de mot de passe</a>

        {% if is_granted('ROLE_ADMIN') %}
            <a href="{{ path('user_index') }}" class="button is-primary">back to list</a>
            {# {{ include('user/_delete_form.html.twig') }} #}
        {%endif%}
    </div>
    
    
{% endblock %}
